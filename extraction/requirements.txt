# EFT extraction kit -- BASE python deps (Python 3.10+; validated on 3.12).
#   pip install -r extraction/requirements.txt
# Optional extras:
#   requirements-bake.txt          GPU SH lighting bake (NVIDIA CUDA only)
#   requirements-legacy-grade.txt  legacy LUT refit only (the authentic LUT ships prebuilt)

UnityPy==1.25.0      # Unity asset reader (all extractors). PINNED: UnityPy's API shifts between minors;
                     # the extractors were written/validated against 1.25.0.
numpy>=1.26          # used everywhere (validated with 2.2.x)
Pillow>=10.0         # texture decode/save (extractors, SH bake albedo sampling, grade LUT)

# NOT needed: requests/pygltflib -- the loot/tasks/icons builders use stdlib urllib only.
# The pack emitter (eft_pipeline/assemble_bevy.py) needs only numpy + Pillow.
